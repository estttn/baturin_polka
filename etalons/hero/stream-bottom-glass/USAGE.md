# stream-bottom-glass

Канон — `PROMPT.md`. Сборки `site/` пока нет.

## Брать

- Видео `fixed` / `object-cover`, z-0
- Нижний blur: `backdrop-blur-xl` + `mask-image: linear-gradient(to top, black 0%, transparent 45%)`. Без затемняющего градиента
- `.liquid-glass` и `blurFadeUp` со stagger
- Контент `flex-1 justify-end`; на md стрелки Previous/Next справа
- Nav: центр-ссылки с `lg`, гамбургер ниже

## Не брать

- Имя CINEMATIC, копирайт фильма, IMDB 8.7 как фейковую метрику на чужом продукте
- Три карточки / логотипы партнёров / скролл ниже hero
