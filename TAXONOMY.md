# Таксономия

Сначала класс, потом эталон. Если класс не сходится — не тянуть ближайший красивый сайт.

## Оси

| Ось | Вопрос | Примеры |
|---|---|---|
| `page` | Поверхность | `landing`, `services`, `case`, `pitch`, `app` |
| `kind` | Бизнес | `studio`, `it-services`, `saas`, `corporate`, `shop` |
| `surface` | Свет | `light`, `dark` |
| `scope` | Масштаб | `fragment` (2–4 блока), `site` (полный набор секций), `system` |
| `sections` | Смысловые блоки в каноне | число. Считать по готовому сайту, не по черновику |
| `motion` | Движение | `none`, `micro`, `shader`, `cinematic` |

## Правила

- Канон — то, что **доделано и лежит в `site/`**. Черновой промпт не важнее сборки.
- `kind: studio` ≠ `kind: it-services`. Сетку и CTA можно взять, оффер и тон — нет.
- Несколько эталонов мешать только явно: «каркас из A, движение из B».

## Идентификаторы

`{kind}-{surface}-{signature}`

Сигнатура — то, без чего паттерн не узнать. Не имя клиента.
